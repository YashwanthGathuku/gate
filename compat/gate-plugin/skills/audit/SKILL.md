---
name: audit
description: Deprecated Gate alias for TheUstad audit-chain verification. Use only when the user invokes $gate:audit.
---

# Gate Compatibility Audit

Resolve this installed package's absolute root from this SKILL.md, resolve an
absolute Python 3.10+ executable, and invoke:

```text
<absolute-python> <plugin-root>/scripts/gate_plugin.py audit <absolute-log-path>
```

Relay the adapter warning, canonical verifier output, and exit code exactly.
